<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permisos para acceder a la ubicación del dispositivo -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- Opción: permiso para ubicación en segundo plano (Android 10+ requiere justificar al usuario) -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GEOFENCEMAPBOX">

        <!-- Actividad de Login -->
        <activity
            android:name=".LoginActivity"
            android:exported="false" />

        <!-- Actividad de Registro -->
        <activity
            android:name=".RegisterActivity"
            android:exported="false" />

        <!-- Actividad Principal (Mapa) -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <!-- Actividad Splash (Punto de Entrada) -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.NoActionBar"> <!-- Un tema sin barra para que se vea limpio -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>